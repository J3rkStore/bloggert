<html lang='en'>

  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Bloggert</title>

    <link rel='stylesheet' href='/css/reset.css' />
    <link rel='stylesheet' href='/css/style.css' />
  </head>

  <body>
    <div>
      <header>
        <h1>
          <span role='img' aria-label='alien'>👽</span>
          <a href='/' class='worm-green'>Bloggert 🛸</a>
        </h1>
        <nav>
          <!-- Conditionally render login or logout links -->
          {{#if logged_in}}
            <a href='/profile' class='worm-green g-button'>profile</a>
            |
            <button
              class='no-button worm-green g-button'
              id='logout'
            >logout</button>
          {{else}}
            <a href='/login' class='worm-green g-button'>login</a>
          {{/if}}
        </nav>
      </header>
      <main>
        <!-- Render the sub layout -->
        {{{body}}}
      </main>
      <footer>

      </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
      <script src='/js/logout.js'></script>
    {{/if}}
  </body>

</html>